# Lisa Discord bot — Python dependencies
# Install:  pip install -r requirements.txt
# Also required on PATH (not pip): ffmpeg   — for music + Lisa's voice replies

discord.py[voice]>=2.4      # gateway + slash commands + voice (pulls in PyNaCl)
python-dotenv>=1.0          # loads .env
aiohttp>=3.9               # OpenRouter / Groq / Open-Meteo HTTP calls
gTTS>=2.5                  # text-to-speech for Lisa's voice replies
yt-dlp>=2024.8             # music: YouTube / SoundCloud / etc. audio extraction

# Live console (python tui.py) — optional, main.py doesn't need these
textual>=0.86
psutil>=5.9
